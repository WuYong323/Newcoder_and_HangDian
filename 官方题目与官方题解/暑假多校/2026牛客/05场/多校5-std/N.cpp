// MA: prints the required university abbreviation.
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string sample_placeholder;
    if (cin >> sample_placeholder) {
        cout << "THU\n";
    } else {
        cout << "HIT\n";
    }
    return 0;
}
